{"componentChunkName":"component---src-pages-simple-marking-required-fields-mdx","path":"/simple/marking-required-fields/","result":{"pageContext":{"frontmatter":{"title":"Marking Fields as Required"},"relativePagePath":"/simple/marking-required-fields.mdx","titleType":"append","MdxNode":{"id":"3b341ece-660e-584b-927f-4d5a2c2f0790","children":[],"parent":"0166eb2f-944b-5c49-90b5-efacfbf40e58","internal":{"content":"---\ntitle: Marking Fields as Required\n---\n\n## About this task\n\nIn Graphite applications, there are several ways to mark fields as required to ensure data integrity and guide users to provide necessary information. This guide explains the different methods available for marking fields as required and provides examples for each approach, organized by common scenarios.\n\n## Scenarios\n\n### Marking fields required at the Datasource level\n\nOne of the most fundamental ways to mark a field as required in a Graphite application is through the datasource attribute's required property. When an attribute in the datasource is marked as required, any UI field bound to that attribute will automatically be treated as required.\n\n**Datasource Configuration:**\n```xml\n<datasource id=\"workOrderDS\">\n  <attribute name=\"description\" required=\"true\" />\n  <attribute name=\"location\" required=\"true\" />\n  <attribute name=\"priority\" />\n</datasource>\n```\n\n**UI Field Binding:**\n```xml\n<text-input id=\"descriptionField\" label=\"Description\" value=\"{datasource.description}\" />\n<text-input id=\"locationField\" label=\"Location\" value=\"{datasource.location}\" />\n```\n\n**What happens:**\n- Fields bound to required attributes will automatically be treated as required\n- An asterisk appears next to the field label\n- Form submission is prevented if the field is empty\n- Validation is handled at the datasource level\n\n**When to use:**\n- When the requirement is driven by the data model\n- For consistent enforcement of required fields across multiple pages\n- When the field should always be required regardless of UI context\n\n### Basic Required Fields\n\nThe simplest scenario is marking fields as permanently required using the `required` attribute in your XML.\n\n**XML Configuration:**\n```xml\n<text-input id=\"description\" label=\"Description\" required=\"true\" />\n<number-input id=\"quantity\" label=\"Quantity\" required=\"true\" />\n<dropdown id=\"status\" label=\"Status\" required=\"true\">\n  <dropdown-item value=\"active\" label=\"Active\" />\n  <dropdown-item value=\"inactive\" label=\"Inactive\" />\n</dropdown>\n```\n\n**What happens:**\n- An asterisk appears next to the field label\n- Form submission is prevented if the field is empty\n- A default error message appears when validation fails\n\n**When to use:**\n- For fields that are always required\n- When using the default validation behavior is sufficient\n- For simple forms with straightforward validation needs\n\n### Custom Error Messages\n\nWhen you need to provide more specific guidance to users about required fields.\n\n**XML Configuration:**\n```xml\n<text-input \n  id=\"assetId\" \n  label=\"Asset ID\" \n  required=\"true\" \n  error-message=\"Please enter a valid Asset ID in the format ASSET-XXXXX\" \n/>\n\n<date-input \n  id=\"dueDate\" \n  label=\"Due Date\" \n  required=\"true\" \n  error-message=\"Due Date is required for scheduling\" \n/>\n```\n\n**JavaScript for dynamic error messages:**\n```js\n/**\n * Get custom error message based on field and context\n * @param {string} fieldId - The field ID\n * @param {Object} context - Validation context\n * @returns {string} - The error message\n */\ngetErrorMessage(fieldId, context) {\n  switch (fieldId) {\n    case 'workOrderId':\n      return 'Work Order ID is required and must be in the format WO-XXXXX';\n    case 'priority':\n      return context.isEmergency \n        ? 'Priority is required for emergency work orders' \n        : 'Please select a priority';\n    default:\n      return 'This field is required';\n  }\n}\n```\n\n**When to use:**\n- When default error messages aren't descriptive enough\n- For fields with specific format requirements\n- When different error contexts need different messages\n\n### Conditionally Required Fields\n\nFor fields that should only be required based on certain conditions.\n\n**XML Configuration:**\n```xml\n<checkbox id=\"emergency\" label=\"Emergency\" checked=\"{page.state.workOrder.emergency}\" on-change=\"handleEmergencyChange\" />\n\n<textarea \n  id=\"notes\" \n  label=\"Notes\" \n  value=\"{page.state.workOrder.notes}\" \n  required=\"{page.state.workOrder.emergency}\"\n  error=\"{page.state.validationErrors.notes}\"\n  on-change=\"handleNotesChange\" \n/>\n\n<dropdown \n  id=\"priority\" \n  label=\"Priority\" \n  value=\"{page.state.workOrder.priority}\" \n  required=\"{page.state.requiredFields.priority}\"\n  error=\"{page.state.validationErrors.priority}\"\n  on-change=\"handlePriorityChange\"\n>\n  <dropdown-item value=\"1\" label=\"High\" />\n  <dropdown-item value=\"2\" label=\"Medium\" />\n  <dropdown-item value=\"3\" label=\"Low\" />\n</dropdown>\n```\n\n**JavaScript Implementation:**\n```js\n/**\n * Handle emergency checkbox change\n * @param {boolean} checked - Whether emergency is checked\n */\nhandleEmergencyChange(checked) {\n  this.page.state.workOrder.emergency = checked;\n  \n  // Update required fields based on emergency status\n  this.updateRequiredFields();\n  \n  // If emergency is checked, priority must be high\n  if (checked && this.page.state.workOrder.priority !== '1') {\n    this.page.state.workOrder.priority = '1';\n  }\n}\n\n/**\n * Update which fields are required based on current state\n */\nupdateRequiredFields() {\n  const workOrder = this.page.state.workOrder;\n  const requiredFields = {\n    description: true,  // Always required\n    location: true,     // Always required\n    priority: workOrder.emergency,  // Required if emergency\n    notes: workOrder.emergency      // Required if emergency\n  };\n  \n  this.page.state.requiredFields = requiredFields;\n}\n```\n\n**When to use:**\n- When fields should only be required in specific circumstances\n- For implementing business rules that affect field requirements\n- When creating dynamic forms that adapt to user input\n\n### Complex Validation Rules\n\nFor scenarios requiring more sophisticated validation beyond simple \"required\" checks.\n\n**JavaScript Implementation:**\n```js\n/**\n * Validate a specific field\n * @param {string} fieldId - The field to validate\n * @returns {boolean} - Whether the field is valid\n */\nvalidateField(fieldId) {\n  const workOrder = this.page.state.workOrder;\n  const requiredFields = this.page.state.requiredFields;\n  const errors = this.page.state.validationErrors || {};\n  \n  // Clear previous error\n  delete errors[fieldId];\n  \n  // Check if field is required and validate accordingly\n  switch (fieldId) {\n    case 'description':\n      if (requiredFields.description && (!workOrder.description || workOrder.description.trim() === '')) {\n        errors[fieldId] = 'Description is required';\n        return false;\n      }\n      break;\n      \n    case 'assetId':\n      if (requiredFields.assetId) {\n        if (!workOrder.assetId) {\n          errors[fieldId] = 'Asset ID is required';\n          return false;\n        }\n        // Check format (e.g., ASSET-12345)\n        if (!/^ASSET-\\d{5}$/.test(workOrder.assetId)) {\n          errors[fieldId] = 'Asset ID must be in format ASSET-XXXXX';\n          return false;\n        }\n      }\n      break;\n      \n    case 'estimatedCost':\n      if (requiredFields.estimatedCost) {\n        if (workOrder.estimatedCost === undefined || workOrder.estimatedCost === null) {\n          errors[fieldId] = 'Estimated cost is required';\n          return false;\n        }\n        // Must be a positive number\n        if (isNaN(workOrder.estimatedCost) || workOrder.estimatedCost <= 0) {\n          errors[fieldId] = 'Estimated cost must be greater than zero';\n          return false;\n        }\n      }\n      break;\n  }\n  \n  // Update validation errors\n  this.page.state.validationErrors = errors;\n  return true;\n}\n```\n\n**When to use:**\n- When validation goes beyond simple presence checks\n- For fields with format requirements (regex patterns)\n- When fields need to be validated against each other\n- For numeric fields with range requirements\n\n### Form-Level Validation\n\nFor validating multiple fields together or implementing cross-field validation.\n\n**JavaScript Implementation:**\n```js\n/**\n * Validate the entire form\n * @returns {boolean} - Whether the form is valid\n */\nvalidateForm() {\n  const workOrder = this.page.state.workOrder;\n  const errors = {};\n  let isValid = true;\n  \n  // Individual field validation\n  if (!workOrder.description || workOrder.description.trim() === '') {\n    errors.description = 'Description is required';\n    isValid = false;\n  }\n  \n  if (!workOrder.location || workOrder.location.trim() === '') {\n    errors.location = 'Location is required';\n    isValid = false;\n  }\n  \n  // Cross-field validation\n  if (workOrder.startDate && workOrder.endDate) {\n    if (new Date(workOrder.startDate) > new Date(workOrder.endDate)) {\n      errors.endDate = 'End date must be after start date';\n      isValid = false;\n    }\n  }\n  \n  // Conditional validation\n  if (workOrder.type === 'preventive' && !workOrder.frequency) {\n    errors.frequency = 'Frequency is required for preventive work orders';\n    isValid = false;\n  }\n  \n  // Update validation errors\n  this.page.state.validationErrors = errors;\n  return isValid;\n}\n\n/**\n * Handle form submission\n */\nhandleSubmit() {\n  if (this.validateForm()) {\n    // Form is valid, proceed with submission\n    this.saveWorkOrder(this.page.state.workOrder);\n  } else {\n    // Show error message\n    this.app.toast('Please correct the errors in the form', 'error');\n  }\n}\n```\n\n**When to use:**\n- When fields need to be validated in relation to each other\n- For implementing business rules that span multiple fields\n- When validation depends on the overall form state\n\n## Complete Example\n\nHere's a complete example showing how to implement required fields with validation for a work order form:\n\n**Page XML:**\n```xml\n<page id=\"workOrderForm\">\n  <state name=\"workOrder\" type=\"object\" default-value=\"{}\" />\n  <state name=\"validationErrors\" type=\"object\" default-value=\"{}\" />\n  <state name=\"requiredFields\" type=\"object\" default-value=\"{description: true, location: true}\" />\n  \n  <page-header>\n    <page-title>Create Work Order</page-title>\n  </page-header>\n  \n  <container>\n    <box>\n      <text-input \n        id=\"description\" \n        label=\"Description\" \n        value=\"{page.state.workOrder.description}\" \n        required=\"true\"\n        error=\"{page.state.validationErrors.description}\"\n        on-change=\"handleDescriptionChange\" \n      />\n      \n      <text-input \n        id=\"location\" \n        label=\"Location\" \n        value=\"{page.state.workOrder.location}\" \n        required=\"true\"\n        error=\"{page.state.validationErrors.location}\"\n        on-change=\"handleLocationChange\" \n      />\n      \n      <checkbox \n        id=\"emergency\" \n        label=\"Emergency\" \n        checked=\"{page.state.workOrder.emergency}\" \n        on-change=\"handleEmergencyChange\" \n      />\n      \n      <dropdown \n        id=\"priority\" \n        label=\"Priority\" \n        value=\"{page.state.workOrder.priority}\" \n        required=\"{page.state.workOrder.emergency}\"\n        error=\"{page.state.validationErrors.priority}\"\n        on-change=\"handlePriorityChange\"\n      >\n        <dropdown-item value=\"1\" label=\"High\" />\n        <dropdown-item value=\"2\" label=\"Medium\" />\n        <dropdown-item value=\"3\" label=\"Low\" />\n      </dropdown>\n      \n      <textarea \n        id=\"notes\" \n        label=\"Notes\" \n        value=\"{page.state.workOrder.notes}\" \n        required=\"{page.state.workOrder.emergency}\"\n        error=\"{page.state.validationErrors.notes}\"\n        on-change=\"handleNotesChange\" \n      />\n      \n      <button label=\"Submit\" on-click=\"handleSubmit\" />\n    </box>\n  </container>\n</page>\n```\n\n**AppCustomizations.js:**\n```js\nimport {Application} from '@maximo/maximo-js-api';\n\nclass AppCustomizations {\n  /**\n   * Initialize page references when the page is initialized\n   * @param {Object} page - The page object\n   * @param {Object} app - The application object\n   */\n  async pageInitialized(page, app) {\n    this.page = page;\n    this.app = app;\n    \n    if (page && page.name === 'workOrderForm') {\n      // Initialize form state\n      this.updateRequiredFields();\n    }\n  }\n\n  /**\n   * Handle description change\n   * @param {string} value - The new description value\n   */\n  handleDescriptionChange(value) {\n    this.page.state.workOrder.description = value;\n    this.validateField('description');\n  }\n\n  /**\n   * Handle location change\n   * @param {string} value - The new location value\n   */\n  handleLocationChange(value) {\n    this.page.state.workOrder.location = value;\n    this.validateField('location');\n  }\n\n  /**\n   * Handle priority change\n   * @param {string} value - The new priority value\n   */\n  handlePriorityChange(value) {\n    this.page.state.workOrder.priority = value;\n    this.validateField('priority');\n  }\n\n  /**\n   * Handle emergency checkbox change\n   * @param {boolean} checked - Whether emergency is checked\n   */\n  handleEmergencyChange(checked) {\n    this.page.state.workOrder.emergency = checked;\n    \n    // Update required fields based on emergency status\n    this.updateRequiredFields();\n    \n    // If emergency is checked, priority must be high\n    if (checked && this.page.state.workOrder.priority !== '1') {\n      this.page.state.workOrder.priority = '1';\n    }\n    \n    // Validate notes field which is now required\n    this.validateField('notes');\n  }\n\n  /**\n   * Handle notes change\n   * @param {string} value - The new notes value\n   */\n  handleNotesChange(value) {\n    this.page.state.workOrder.notes = value;\n    this.validateField('notes');\n  }\n\n  /**\n   * Update which fields are required based on current state\n   */\n  updateRequiredFields() {\n    const workOrder = this.page.state.workOrder;\n    const requiredFields = {\n      description: true,  // Always required\n      location: true,     // Always required\n      priority: workOrder.emergency,  // Required if emergency\n      notes: workOrder.emergency      // Required if emergency\n    };\n    \n    this.page.state.requiredFields = requiredFields;\n  }\n\n  /**\n   * Validate a specific field\n   * @param {string} fieldId - The field to validate\n   * @returns {boolean} - Whether the field is valid\n   */\n  validateField(fieldId) {\n    const workOrder = this.page.state.workOrder;\n    const requiredFields = this.page.state.requiredFields;\n    const errors = this.page.state.validationErrors || {};\n    \n    // Clear previous error\n    delete errors[fieldId];\n    \n    // Check if field is required and empty\n    if (requiredFields[fieldId]) {\n      switch (fieldId) {\n        case 'description':\n          if (!workOrder.description || workOrder.description.trim() === '') {\n            errors[fieldId] = 'Description is required';\n            return false;\n          }\n          break;\n          \n        case 'location':\n          if (!workOrder.location || workOrder.location.trim() === '') {\n            errors[fieldId] = 'Location is required';\n            return false;\n          }\n          break;\n          \n        case 'priority':\n          if (!workOrder.priority) {\n            errors[fieldId] = 'Priority is required';\n            return false;\n          }\n          break;\n          \n        case 'notes':\n          if (workOrder.emergency && (!workOrder.notes || workOrder.notes.trim() === '')) {\n            errors[fieldId] = 'Notes are required for emergency work orders';\n            return false;\n          }\n          break;\n      }\n    }\n    \n    // Update validation errors\n    this.page.state.validationErrors = errors;\n    return true;\n  }\n\n  /**\n   * Validate the entire form\n   * @returns {boolean} - Whether the form is valid\n   */\n  validateForm() {\n    const fields = ['description', 'location', 'priority', 'notes'];\n    let isValid = true;\n    \n    // Validate each field\n    fields.forEach(field => {\n      if (!this.validateField(field)) {\n        isValid = false;\n      }\n    });\n    \n    return isValid;\n  }\n\n  /**\n   * Handle form submission\n   */\n  handleSubmit() {\n    if (this.validateForm()) {\n      // Form is valid, proceed with submission\n      this.app.toast('Work order submitted successfully', 'success');\n      \n      // In a real application, you would save the data here\n      this.saveWorkOrder(this.page.state.workOrder);\n    } else {\n      // Show error message\n      this.app.toast('Please correct the errors in the form', 'error');\n    }\n  }\n\n  /**\n   * Save work order to the server\n   * @param {Object} workOrder - The work order data\n   */\n  async saveWorkOrder(workOrder) {\n    try {\n      // In a real application, this would be an API call\n      console.log('Saving work order:', workOrder);\n      \n      // Simulate server call\n      await new Promise(resolve => setTimeout(resolve, 1000));\n      \n      // Reset form after successful save\n      this.page.state.workOrder = {};\n    } catch (error) {\n      console.error('Failed to save work order', error);\n      this.app.toast('Failed to save work order', 'error');\n    }\n  }\n}\n\nexport default AppCustomizations;\n```\n\n## Best Practices and Considerations\n\n- **Validation Timing**: Consider when to validate - on submit, on blur, or as the user types.\n- **Error Messages**: Provide specific, actionable error messages rather than generic ones.\n- **Accessibility**: Ensure error states are accessible to screen readers and other assistive technologies.\n- **Conditional Requirements**: Clearly indicate when fields become required based on other selections.\n- **Performance**: For large forms, consider validating fields individually rather than all at once.\n- **Default Values**: Provide sensible default values where appropriate to reduce the burden on users.\n- **Error Recovery**: Make it easy for users to correct errors without losing other entered data.\n- **Mobile Considerations**: Ensure validation works well on mobile devices with appropriate error placement.\n- **Testing**: Test required field validation with various input scenarios, including edge cases.","type":"Mdx","contentDigest":"12d3ecfc8d8a1879af8a8ff3f500938b","owner":"gatsby-plugin-mdx","counter":178},"frontmatter":{"title":"Marking Fields as Required"},"exports":{},"rawBody":"---\ntitle: Marking Fields as Required\n---\n\n## About this task\n\nIn Graphite applications, there are several ways to mark fields as required to ensure data integrity and guide users to provide necessary information. This guide explains the different methods available for marking fields as required and provides examples for each approach, organized by common scenarios.\n\n## Scenarios\n\n### Marking fields required at the Datasource level\n\nOne of the most fundamental ways to mark a field as required in a Graphite application is through the datasource attribute's required property. When an attribute in the datasource is marked as required, any UI field bound to that attribute will automatically be treated as required.\n\n**Datasource Configuration:**\n```xml\n<datasource id=\"workOrderDS\">\n  <attribute name=\"description\" required=\"true\" />\n  <attribute name=\"location\" required=\"true\" />\n  <attribute name=\"priority\" />\n</datasource>\n```\n\n**UI Field Binding:**\n```xml\n<text-input id=\"descriptionField\" label=\"Description\" value=\"{datasource.description}\" />\n<text-input id=\"locationField\" label=\"Location\" value=\"{datasource.location}\" />\n```\n\n**What happens:**\n- Fields bound to required attributes will automatically be treated as required\n- An asterisk appears next to the field label\n- Form submission is prevented if the field is empty\n- Validation is handled at the datasource level\n\n**When to use:**\n- When the requirement is driven by the data model\n- For consistent enforcement of required fields across multiple pages\n- When the field should always be required regardless of UI context\n\n### Basic Required Fields\n\nThe simplest scenario is marking fields as permanently required using the `required` attribute in your XML.\n\n**XML Configuration:**\n```xml\n<text-input id=\"description\" label=\"Description\" required=\"true\" />\n<number-input id=\"quantity\" label=\"Quantity\" required=\"true\" />\n<dropdown id=\"status\" label=\"Status\" required=\"true\">\n  <dropdown-item value=\"active\" label=\"Active\" />\n  <dropdown-item value=\"inactive\" label=\"Inactive\" />\n</dropdown>\n```\n\n**What happens:**\n- An asterisk appears next to the field label\n- Form submission is prevented if the field is empty\n- A default error message appears when validation fails\n\n**When to use:**\n- For fields that are always required\n- When using the default validation behavior is sufficient\n- For simple forms with straightforward validation needs\n\n### Custom Error Messages\n\nWhen you need to provide more specific guidance to users about required fields.\n\n**XML Configuration:**\n```xml\n<text-input \n  id=\"assetId\" \n  label=\"Asset ID\" \n  required=\"true\" \n  error-message=\"Please enter a valid Asset ID in the format ASSET-XXXXX\" \n/>\n\n<date-input \n  id=\"dueDate\" \n  label=\"Due Date\" \n  required=\"true\" \n  error-message=\"Due Date is required for scheduling\" \n/>\n```\n\n**JavaScript for dynamic error messages:**\n```js\n/**\n * Get custom error message based on field and context\n * @param {string} fieldId - The field ID\n * @param {Object} context - Validation context\n * @returns {string} - The error message\n */\ngetErrorMessage(fieldId, context) {\n  switch (fieldId) {\n    case 'workOrderId':\n      return 'Work Order ID is required and must be in the format WO-XXXXX';\n    case 'priority':\n      return context.isEmergency \n        ? 'Priority is required for emergency work orders' \n        : 'Please select a priority';\n    default:\n      return 'This field is required';\n  }\n}\n```\n\n**When to use:**\n- When default error messages aren't descriptive enough\n- For fields with specific format requirements\n- When different error contexts need different messages\n\n### Conditionally Required Fields\n\nFor fields that should only be required based on certain conditions.\n\n**XML Configuration:**\n```xml\n<checkbox id=\"emergency\" label=\"Emergency\" checked=\"{page.state.workOrder.emergency}\" on-change=\"handleEmergencyChange\" />\n\n<textarea \n  id=\"notes\" \n  label=\"Notes\" \n  value=\"{page.state.workOrder.notes}\" \n  required=\"{page.state.workOrder.emergency}\"\n  error=\"{page.state.validationErrors.notes}\"\n  on-change=\"handleNotesChange\" \n/>\n\n<dropdown \n  id=\"priority\" \n  label=\"Priority\" \n  value=\"{page.state.workOrder.priority}\" \n  required=\"{page.state.requiredFields.priority}\"\n  error=\"{page.state.validationErrors.priority}\"\n  on-change=\"handlePriorityChange\"\n>\n  <dropdown-item value=\"1\" label=\"High\" />\n  <dropdown-item value=\"2\" label=\"Medium\" />\n  <dropdown-item value=\"3\" label=\"Low\" />\n</dropdown>\n```\n\n**JavaScript Implementation:**\n```js\n/**\n * Handle emergency checkbox change\n * @param {boolean} checked - Whether emergency is checked\n */\nhandleEmergencyChange(checked) {\n  this.page.state.workOrder.emergency = checked;\n  \n  // Update required fields based on emergency status\n  this.updateRequiredFields();\n  \n  // If emergency is checked, priority must be high\n  if (checked && this.page.state.workOrder.priority !== '1') {\n    this.page.state.workOrder.priority = '1';\n  }\n}\n\n/**\n * Update which fields are required based on current state\n */\nupdateRequiredFields() {\n  const workOrder = this.page.state.workOrder;\n  const requiredFields = {\n    description: true,  // Always required\n    location: true,     // Always required\n    priority: workOrder.emergency,  // Required if emergency\n    notes: workOrder.emergency      // Required if emergency\n  };\n  \n  this.page.state.requiredFields = requiredFields;\n}\n```\n\n**When to use:**\n- When fields should only be required in specific circumstances\n- For implementing business rules that affect field requirements\n- When creating dynamic forms that adapt to user input\n\n### Complex Validation Rules\n\nFor scenarios requiring more sophisticated validation beyond simple \"required\" checks.\n\n**JavaScript Implementation:**\n```js\n/**\n * Validate a specific field\n * @param {string} fieldId - The field to validate\n * @returns {boolean} - Whether the field is valid\n */\nvalidateField(fieldId) {\n  const workOrder = this.page.state.workOrder;\n  const requiredFields = this.page.state.requiredFields;\n  const errors = this.page.state.validationErrors || {};\n  \n  // Clear previous error\n  delete errors[fieldId];\n  \n  // Check if field is required and validate accordingly\n  switch (fieldId) {\n    case 'description':\n      if (requiredFields.description && (!workOrder.description || workOrder.description.trim() === '')) {\n        errors[fieldId] = 'Description is required';\n        return false;\n      }\n      break;\n      \n    case 'assetId':\n      if (requiredFields.assetId) {\n        if (!workOrder.assetId) {\n          errors[fieldId] = 'Asset ID is required';\n          return false;\n        }\n        // Check format (e.g., ASSET-12345)\n        if (!/^ASSET-\\d{5}$/.test(workOrder.assetId)) {\n          errors[fieldId] = 'Asset ID must be in format ASSET-XXXXX';\n          return false;\n        }\n      }\n      break;\n      \n    case 'estimatedCost':\n      if (requiredFields.estimatedCost) {\n        if (workOrder.estimatedCost === undefined || workOrder.estimatedCost === null) {\n          errors[fieldId] = 'Estimated cost is required';\n          return false;\n        }\n        // Must be a positive number\n        if (isNaN(workOrder.estimatedCost) || workOrder.estimatedCost <= 0) {\n          errors[fieldId] = 'Estimated cost must be greater than zero';\n          return false;\n        }\n      }\n      break;\n  }\n  \n  // Update validation errors\n  this.page.state.validationErrors = errors;\n  return true;\n}\n```\n\n**When to use:**\n- When validation goes beyond simple presence checks\n- For fields with format requirements (regex patterns)\n- When fields need to be validated against each other\n- For numeric fields with range requirements\n\n### Form-Level Validation\n\nFor validating multiple fields together or implementing cross-field validation.\n\n**JavaScript Implementation:**\n```js\n/**\n * Validate the entire form\n * @returns {boolean} - Whether the form is valid\n */\nvalidateForm() {\n  const workOrder = this.page.state.workOrder;\n  const errors = {};\n  let isValid = true;\n  \n  // Individual field validation\n  if (!workOrder.description || workOrder.description.trim() === '') {\n    errors.description = 'Description is required';\n    isValid = false;\n  }\n  \n  if (!workOrder.location || workOrder.location.trim() === '') {\n    errors.location = 'Location is required';\n    isValid = false;\n  }\n  \n  // Cross-field validation\n  if (workOrder.startDate && workOrder.endDate) {\n    if (new Date(workOrder.startDate) > new Date(workOrder.endDate)) {\n      errors.endDate = 'End date must be after start date';\n      isValid = false;\n    }\n  }\n  \n  // Conditional validation\n  if (workOrder.type === 'preventive' && !workOrder.frequency) {\n    errors.frequency = 'Frequency is required for preventive work orders';\n    isValid = false;\n  }\n  \n  // Update validation errors\n  this.page.state.validationErrors = errors;\n  return isValid;\n}\n\n/**\n * Handle form submission\n */\nhandleSubmit() {\n  if (this.validateForm()) {\n    // Form is valid, proceed with submission\n    this.saveWorkOrder(this.page.state.workOrder);\n  } else {\n    // Show error message\n    this.app.toast('Please correct the errors in the form', 'error');\n  }\n}\n```\n\n**When to use:**\n- When fields need to be validated in relation to each other\n- For implementing business rules that span multiple fields\n- When validation depends on the overall form state\n\n## Complete Example\n\nHere's a complete example showing how to implement required fields with validation for a work order form:\n\n**Page XML:**\n```xml\n<page id=\"workOrderForm\">\n  <state name=\"workOrder\" type=\"object\" default-value=\"{}\" />\n  <state name=\"validationErrors\" type=\"object\" default-value=\"{}\" />\n  <state name=\"requiredFields\" type=\"object\" default-value=\"{description: true, location: true}\" />\n  \n  <page-header>\n    <page-title>Create Work Order</page-title>\n  </page-header>\n  \n  <container>\n    <box>\n      <text-input \n        id=\"description\" \n        label=\"Description\" \n        value=\"{page.state.workOrder.description}\" \n        required=\"true\"\n        error=\"{page.state.validationErrors.description}\"\n        on-change=\"handleDescriptionChange\" \n      />\n      \n      <text-input \n        id=\"location\" \n        label=\"Location\" \n        value=\"{page.state.workOrder.location}\" \n        required=\"true\"\n        error=\"{page.state.validationErrors.location}\"\n        on-change=\"handleLocationChange\" \n      />\n      \n      <checkbox \n        id=\"emergency\" \n        label=\"Emergency\" \n        checked=\"{page.state.workOrder.emergency}\" \n        on-change=\"handleEmergencyChange\" \n      />\n      \n      <dropdown \n        id=\"priority\" \n        label=\"Priority\" \n        value=\"{page.state.workOrder.priority}\" \n        required=\"{page.state.workOrder.emergency}\"\n        error=\"{page.state.validationErrors.priority}\"\n        on-change=\"handlePriorityChange\"\n      >\n        <dropdown-item value=\"1\" label=\"High\" />\n        <dropdown-item value=\"2\" label=\"Medium\" />\n        <dropdown-item value=\"3\" label=\"Low\" />\n      </dropdown>\n      \n      <textarea \n        id=\"notes\" \n        label=\"Notes\" \n        value=\"{page.state.workOrder.notes}\" \n        required=\"{page.state.workOrder.emergency}\"\n        error=\"{page.state.validationErrors.notes}\"\n        on-change=\"handleNotesChange\" \n      />\n      \n      <button label=\"Submit\" on-click=\"handleSubmit\" />\n    </box>\n  </container>\n</page>\n```\n\n**AppCustomizations.js:**\n```js\nimport {Application} from '@maximo/maximo-js-api';\n\nclass AppCustomizations {\n  /**\n   * Initialize page references when the page is initialized\n   * @param {Object} page - The page object\n   * @param {Object} app - The application object\n   */\n  async pageInitialized(page, app) {\n    this.page = page;\n    this.app = app;\n    \n    if (page && page.name === 'workOrderForm') {\n      // Initialize form state\n      this.updateRequiredFields();\n    }\n  }\n\n  /**\n   * Handle description change\n   * @param {string} value - The new description value\n   */\n  handleDescriptionChange(value) {\n    this.page.state.workOrder.description = value;\n    this.validateField('description');\n  }\n\n  /**\n   * Handle location change\n   * @param {string} value - The new location value\n   */\n  handleLocationChange(value) {\n    this.page.state.workOrder.location = value;\n    this.validateField('location');\n  }\n\n  /**\n   * Handle priority change\n   * @param {string} value - The new priority value\n   */\n  handlePriorityChange(value) {\n    this.page.state.workOrder.priority = value;\n    this.validateField('priority');\n  }\n\n  /**\n   * Handle emergency checkbox change\n   * @param {boolean} checked - Whether emergency is checked\n   */\n  handleEmergencyChange(checked) {\n    this.page.state.workOrder.emergency = checked;\n    \n    // Update required fields based on emergency status\n    this.updateRequiredFields();\n    \n    // If emergency is checked, priority must be high\n    if (checked && this.page.state.workOrder.priority !== '1') {\n      this.page.state.workOrder.priority = '1';\n    }\n    \n    // Validate notes field which is now required\n    this.validateField('notes');\n  }\n\n  /**\n   * Handle notes change\n   * @param {string} value - The new notes value\n   */\n  handleNotesChange(value) {\n    this.page.state.workOrder.notes = value;\n    this.validateField('notes');\n  }\n\n  /**\n   * Update which fields are required based on current state\n   */\n  updateRequiredFields() {\n    const workOrder = this.page.state.workOrder;\n    const requiredFields = {\n      description: true,  // Always required\n      location: true,     // Always required\n      priority: workOrder.emergency,  // Required if emergency\n      notes: workOrder.emergency      // Required if emergency\n    };\n    \n    this.page.state.requiredFields = requiredFields;\n  }\n\n  /**\n   * Validate a specific field\n   * @param {string} fieldId - The field to validate\n   * @returns {boolean} - Whether the field is valid\n   */\n  validateField(fieldId) {\n    const workOrder = this.page.state.workOrder;\n    const requiredFields = this.page.state.requiredFields;\n    const errors = this.page.state.validationErrors || {};\n    \n    // Clear previous error\n    delete errors[fieldId];\n    \n    // Check if field is required and empty\n    if (requiredFields[fieldId]) {\n      switch (fieldId) {\n        case 'description':\n          if (!workOrder.description || workOrder.description.trim() === '') {\n            errors[fieldId] = 'Description is required';\n            return false;\n          }\n          break;\n          \n        case 'location':\n          if (!workOrder.location || workOrder.location.trim() === '') {\n            errors[fieldId] = 'Location is required';\n            return false;\n          }\n          break;\n          \n        case 'priority':\n          if (!workOrder.priority) {\n            errors[fieldId] = 'Priority is required';\n            return false;\n          }\n          break;\n          \n        case 'notes':\n          if (workOrder.emergency && (!workOrder.notes || workOrder.notes.trim() === '')) {\n            errors[fieldId] = 'Notes are required for emergency work orders';\n            return false;\n          }\n          break;\n      }\n    }\n    \n    // Update validation errors\n    this.page.state.validationErrors = errors;\n    return true;\n  }\n\n  /**\n   * Validate the entire form\n   * @returns {boolean} - Whether the form is valid\n   */\n  validateForm() {\n    const fields = ['description', 'location', 'priority', 'notes'];\n    let isValid = true;\n    \n    // Validate each field\n    fields.forEach(field => {\n      if (!this.validateField(field)) {\n        isValid = false;\n      }\n    });\n    \n    return isValid;\n  }\n\n  /**\n   * Handle form submission\n   */\n  handleSubmit() {\n    if (this.validateForm()) {\n      // Form is valid, proceed with submission\n      this.app.toast('Work order submitted successfully', 'success');\n      \n      // In a real application, you would save the data here\n      this.saveWorkOrder(this.page.state.workOrder);\n    } else {\n      // Show error message\n      this.app.toast('Please correct the errors in the form', 'error');\n    }\n  }\n\n  /**\n   * Save work order to the server\n   * @param {Object} workOrder - The work order data\n   */\n  async saveWorkOrder(workOrder) {\n    try {\n      // In a real application, this would be an API call\n      console.log('Saving work order:', workOrder);\n      \n      // Simulate server call\n      await new Promise(resolve => setTimeout(resolve, 1000));\n      \n      // Reset form after successful save\n      this.page.state.workOrder = {};\n    } catch (error) {\n      console.error('Failed to save work order', error);\n      this.app.toast('Failed to save work order', 'error');\n    }\n  }\n}\n\nexport default AppCustomizations;\n```\n\n## Best Practices and Considerations\n\n- **Validation Timing**: Consider when to validate - on submit, on blur, or as the user types.\n- **Error Messages**: Provide specific, actionable error messages rather than generic ones.\n- **Accessibility**: Ensure error states are accessible to screen readers and other assistive technologies.\n- **Conditional Requirements**: Clearly indicate when fields become required based on other selections.\n- **Performance**: For large forms, consider validating fields individually rather than all at once.\n- **Default Values**: Provide sensible default values where appropriate to reduce the burden on users.\n- **Error Recovery**: Make it easy for users to correct errors without losing other entered data.\n- **Mobile Considerations**: Ensure validation works well on mobile devices with appropriate error placement.\n- **Testing**: Test required field validation with various input scenarios, including edge cases.","fileAbsolutePath":"/work/configuration-practices-documentation/src/pages/simple/marking-required-fields.mdx"}}},"staticQueryHashes":["1364590287","137577622","2102389209","2456312558","2746626797","3018647132","3037994772","768070550"]}